# Public Raw Source Notice

Raw operator captures, including system-prompt exports and account-specific
session material, are intentionally omitted from this public release.

In the private vault, `.raw/` is the immutable source store for captured
operator evidence. In this public copy, the directory exists only to document
that boundary and to satisfy vault checks without exposing raw captures.
